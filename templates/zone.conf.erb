; BIND reverse data file for empty rfc1918 zone
;
; DO NOT EDIT THIS FILE - it is used for multiple zones.
; Instead, copy it, edit named.conf, and use that copy.
;
$TTL    86400
@       IN      SOA     ns.<%= @domain_name %>. root.<%= @domain_name %>. (
                              1         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                          86400 )       ; Negative Cache TTL
;
@               IN      NS      ns.<%= @domain_name %>.

<% @hosts.each do |name, data| -%>
<%= name %>	IN	A		<%= @network_prefix %><%= data['ip'] %>
<% end %>
ns              IN      A       <%= @network_prefix %><%= @ns_ip %>
gateway         IN      A       <%= @network_prefix %><%= @gateway_ip %>
alkibox			IN		CNAME	ns
puppet  		IN		CNAME	ns
backup  		IN		CNAME	ns
ldap            IN      CNAME   ns
cloud           IN      CNAME   ns
